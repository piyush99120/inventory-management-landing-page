import { FloatingNavDemo } from "./ui/floating-navbar-demo"; // Import the FloatingNavDemo component

const Header = () => {
  return (
    <>
      <FloatingNavDemo /> {/* Keep only the FloatingNavDemo component */}
    </>
  );
};

export default Header;
